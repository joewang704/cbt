import React, { useEffect } from 'react'
import { connect } from 'react-redux'

function FacingFears() {
  return <div>
    </div>
}

export default connect(
  (state) => {
    return {
    }
  },
  dispatch => ({
  })
)(FacingFears)